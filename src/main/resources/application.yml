server:
  address: ${APP_HOST:0.0.0.0}
  port: ${APP_PORT:8888}

app:
  qoder-pat: ${QODER_PAT:}
  host: ${APP_HOST:0.0.0.0}
  port: ${APP_PORT:8888}
  upstream-timeout-seconds: ${QODER_UPSTREAM_TIMEOUT_SECONDS:60}
